version: "3.8"

services:
  trading-bot:
    build: .
    command: python main_updated_final.py
    env_file: .env
    volumes:
      - ./data:/app/data

  security:
    build: .
    command: python security_bot_final.py
    env_file: .env
    volumes:
      - ./data:/app/data
